__pycache__/
*.py[cod]
*.egg-info/

# Downloaded media (yt-dlp / IG / TikTok / user uploads)
v1/downloads/
v1/uploads/

# yt-dlp / Whisper caches and temp logs
*.ytdlp.log
*.part

# Local secrets if they ever appear (we currently take Gemini key via UI only)
.env
.env.*

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Editor backups
*.bak

# Caption history sidecars (per-clip, regenerable)
**/.captions/
**/.frames/
**/.thumb*.jpg
**/.thumb*.png

# Local Claude Agent SDK skills cache — copied here only as an experiment;
# caption.py disabled skill loading, so don't ship them in git.
v1/.claude/
v1/caption-sdk.log
